{
  "name": "mgrnz-blog",
  "version": "1.0.0",
  "description": "Hugo blog with admin system",
  "scripts": {
    "build": "hugo --gc --minify",
    "dev": "hugo server",
    "clean": "rm -rf public"
  },
  "engines": {
    "node": "18"
  }
}